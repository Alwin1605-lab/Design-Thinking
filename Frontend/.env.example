VITE_GOOGLE_MAPS_API_KEY=

# Firebase keys removed from the project. If you previously used Firebase,
# remove those values and keep secrets out of version control. The frontend
# no longer requires Firebase configuration for this project.
