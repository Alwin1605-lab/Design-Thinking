// NotificationsToggle: removed (Firebase/FCM is no longer used). Keep a tiny
// stub component so imports from older UI branches do not break.
import React from 'react';

export default function NotificationsToggle() {
  // Notifications were intentionally removed. UI should not show any toggle.
  return null;
}
